@model IEnumerable<UserListViewModel>

@{
    ViewData["Title"] = "Manage Users";
}


<h2 class="heading-margin text-center">@ViewBag.Title</h2>

<table class="table table-hover table-primary table-striped">
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Action</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Name</td>
                <td>@user.Email</td>
                <td>
                    <a asp-area="admin" asp-action="edit" asp-controller="user" asp-route-id="@user.Id" class="btn btn-light">Edit</a>
                    <a asp-area="admin" asp-action="roles" asp-controller="user" asp-route-id="@user.Id" class="btn btn-warning">Roles</a>
                </td>                
            </tr>
        }
    </tbody>
</table>